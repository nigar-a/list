<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^public
    RewriteRule ^(.*)$ public/$1 [L]
    # Redirect all normal pages to https pages (http => https)
    RewriteCond %{HTTPS} ^off$ [NC]
    RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1  [R=301,L]w
</IfModule>